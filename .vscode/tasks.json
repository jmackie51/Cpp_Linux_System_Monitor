{
    "tasks": [
        {
            "type": "shell",
            "label": "g++ - build active file",
            "command": "/usr/bin/make",
            "args": [
                "debug"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
        }
    ],
    "version": "2.0.0"
}